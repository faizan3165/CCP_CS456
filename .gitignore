# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Trained model weights (~43 MB, regenerated by run_all.py) — keep the repo light
*.pth

# NOTE: data/ (real, cited input images), outputs/*.png and report/ ARE committed —
# they are graded deliverables. Every image is real and publicly licensed (README §4).

# OS / editor
.DS_Store
.idea/
.vscode/
